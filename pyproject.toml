[tool.poetry]
name = "HohohoCode"
version = "0.1.0"
description = "Advent of Code for all years"
authors = ["me"]

[tool.poetry.dependencies]
python = "^3.11.0"
ipykernel = "^6.27.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
